Questions

- logR Fallback ok?
- where should i get logR?
- gaia params ok?

Channels:
- IR Channel? NIR does not have a detector i gather from the proposal. Right now i blindly implemented all three channels as if they were the same. what do we do with IR? i also calculated counts per s per pixel. which s wrong i guess. So from then on, i should handle that differently

- FWHM vs resolution scale as pixel_sclae * 2
-- In the meeting you said the instrumental resolution would be provided as FWHM and that I should pass it to the Gaussian broadening. In the legacy code you actually pass FWHM divided by two into gaussbroad, since that function expects HWHM.
-- In my implementation I currently pass the pixel scale directly as HWHM. That is equivalent as long as FWHM equals two times the pixel scale.
-- At the moment I am not using the resolution factor from the channel config, so the effective resolution is implicitly fixed through the pixel scale. I just want to confirm that this assumption is correct for all channels going forward.