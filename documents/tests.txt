We now have working fixtures and domain tests.

Next step is to refactor logic and integration tests to use these fixtures consistently.

Rules:

never introduce strings or config text unless the test explicitly tests parsing

always prefer real objects via fixtures

no SimpleNamespace, no monkeypatch unless strictly necessary

keep existing comments above tests

minimal changes, only what is needed

tell me explicitly what to remove, what to replace, and what to keep

if fixture usage does not make sense for a test, say so clearly

ask for production code if needed before making assumptions

Goal:

eliminate fragile test setup

make tests refactor-safe

use domain-consistent objects everywhere

Also:

introduce testfixtures where it actually adds value

do not use testfixtures blindly

Start with this file:
[paste next test file]

About testfixtures, because this is where people mess it up:

Use it for:

1
Replacing external dependencies cleanly
instead of monkeypatch

2
Capturing logs
instead of caplog if you want stricter control

3
Temporary directory and file comparisons
when you care about exact output

4
Comparing complex structures
when plain assert gets messy

Do NOT use it for:

building domain objects
→ that is what your fixtures are for

replacing simple values
→ overrides in fixture factories are cleaner

anything that makes tests less readable

Your current architecture is:

fixtures = object creation
testfixtures = environment control

Keep that separation.

We now continue refactoring test files.

Context:

fixtures are the single source of truth for domain objects
no SimpleNamespace
no ad-hoc object construction
snapshot tests stay untouched except for minimal fixes
orchestration tests may mock internal steps
unit tests stay simple and use raw data when appropriate

Rules:

never introduce strings or config text unless testing parsing
always prefer fixtures for domain objects
use testfixtures only for environment control, not object creation
keep existing comments above tests
minimal changes only
do not rewrite tests unnecessarily
do not change working logic
if something is unclear, ask before changing

Output format:

what to remove
what to replace
what to keep
exact code only where needed

If fixture usage does not make sense, say so clearly

Write or refactor tests using this exact comment style:

Tests: <function_name>
Behavior: <what is being tested in one short sentence>

Rules:
keep comments short, max two lines
no explanations, no essays
no mention of fixtures or setup
only describe observable behavior
keep existing comments unless wrong
fix only incorrect comments
no extra commentary in output

i will start now giving you the testfixtures.. read them. NO SUMMARY. ONLY EVER MANAGEMENT SUMMARIES. no extensive explanations unless i ask. no code unless i ask