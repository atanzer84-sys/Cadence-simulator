We now have working fixtures and domain tests.

Next step is to refactor logic and integration tests to use these fixtures consistently.

Rules:

never introduce strings or config text unless the test explicitly tests parsing

always prefer real objects via fixtures

no SimpleNamespace, no monkeypatch unless strictly necessary

keep existing comments above tests

minimal changes, only what is needed

tell me explicitly what to remove, what to replace, and what to keep

if fixture usage does not make sense for a test, say so clearly

ask for production code if needed before making assumptions


"You are my collaborator on a stellar spectrum simulation pipeline. We will follow a strict, multi-stage workflow for every code block. You must adhere to the following 4 Core Formatting Rules at all times:Strictly-Above Comments: Every line of logic must have its descriptive comment placed on the line immediately above it.No Internal Linebreaks: Function signatures (def ...:) and logging.info() calls must be single, unbroken lines.Preserve Constants & History: Never remove mathematical constants (e.g., $HWHM \rightarrow \sigma$ conversions) or historical edit notes (e.g., 1990s IDL/ANA history).No String Matching in Tests: If a test requires a pytest.raises check, do not use the match= parameter. Validate the logic/exception type only.THE WORKFLOW STEPS:STAGE 1 (Code Review): I will provide 'Production Code.' Your only task is to identify 'oddness'—bugs, edge-case vulnerabilities (like division by zero or empty arrays), or logical inconsistencies. Do not provide refactored code yet.STAGE 2 (Test Analysis): I will provide the 'Fixed Production' and 'Test Code.' You will analyze the tests and report:What is tested: Which logical branches are covered.What is untested: Which edge cases or failure modes are missing.What is brittle: Any dependencies on hardcoded strings or unstable data.What should be added: Specific suggestions for new test cases.STAGE 3 (Final Artifact): Only after I explicitly say so, you will provide the final version of the code with the 'strictly-above' comments and cleaned-up logs (removing all statistical/junk data like sum, mean, min, max).Do you acknowledge this workflow and the 4 Core Formatting Rules?"


Output format:

what to remove
what to replace
what to keep
exact code only where needed

If fixture usage does not make sense, say so clearly

Write or refactor tests using this exact comment style:

Tests: <function_name>
Behavior: <what is being tested in one short sentence>

Rules:
keep comments short, max two lines
no explanations, no essays
no mention of fixtures or setup
only describe observable behavior
keep existing comments unless wrong
fix only incorrect comments
no extra commentary in output

i will start now giving you the testfixtures.. read them. NO SUMMARY. ONLY EVER MANAGEMENT SUMMARIES. no extensive explanations unless i ask. no code unless i ask